"use client"

// This is the same login page we created earlier
// We're just moving it to the (auth) route group
// The content remains the same

export default function LoginPage() {
  return (
    <div>
      <h1>Login Page</h1>
      <p>This is the login page.</p>
    </div>
  )
}
