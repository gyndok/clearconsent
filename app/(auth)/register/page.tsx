"use client"

// This is the same registration page we created earlier
// We're just moving it to the (auth) route group
// The content remains the same

export default function RegisterPage() {
  return (
    <div>
      <h1>Register</h1>
      <p>This is the registration page.</p>
    </div>
  )
}
